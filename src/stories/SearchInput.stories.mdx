import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import SearchInput from '../components/SearchInput';

<Meta title='MDX/SearchInput' component={SearchInput} />

export const Template = (args) => <SearchInput {...args} />;

<Canvas>
  <Story
    name='Empty'
    args={{
      placeholderText: 'Search value',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name='with value'
    args={{
      searchValue: 'search',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
